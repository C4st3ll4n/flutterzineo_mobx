name: flutterzineo_mobx
description: A new Flutter application.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  mobx:
  cupertino_icons: ^0.1.3
  flutter_mobx:

dev_dependencies:
  mobx_codegen: ^1.1.1+1
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true
